<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SLA_Target_DateTime__c</fullName>
    <description>BR-10. Response deadline established once at intake from Segment_Band__mdt. WRITE-ONCE: never recalculated, so a later configuration change cannot retarget existing Leads. Blank means no SLA was established - never a guessed deadline.</description>
    <externalId>false</externalId>
    <inlineHelpText>Response deadline, set once when the Lead arrived. Not recalculated.</inlineHelpText>
    <label>SLA Target</label>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>DateTime</type>
</CustomField>
