# NorthstarIQ - Revenue Operations Intelligence Platform
# Issue template configuration.
#
# NOTE (Phase 0A): `contact_links` are intentionally omitted.
# GitHub requires absolute URLs here, and the remote repository does not exist yet -
# the GitHub account/owner is not yet established (see the Phase 0A open decisions).
# Guessing an owner slug would produce broken links, so they are added at repository
# creation time (Phase 0 push gate) rather than invented now.
#
# Key documents until then:
#   CLAUDE.md                                          engineering contract & guardrails
#   docs/README.md                                     documentation index
#   docs/governance/naming-conventions.md              canonical names & identifiers
#   docs/governance/implementation-status-conventions.md   status & evidence vocabulary

blank_issues_enabled: true
